Vibe-coded port of `pycocotools` to perform evaluation of object detection in rust.

Ported by opus 4.5, but cleaned up by human hands.

It matches python eval version closely, but not exactly.

Maybe I'll add comparison numbers here, maybe...
